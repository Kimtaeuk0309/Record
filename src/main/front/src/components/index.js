import React from 'react';
import ReactDOM from 'react-dom/client';
import WallRecordSystem from './components/WallRecordSystem/WallRecordSystem';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <WallRecordSystem />
  </React.StrictMode>
);
